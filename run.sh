#!/bin/bash

RUN_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )" # the directory that this script is in 
source "$RUN_DIR/src/utils/logger.sh"
source "$RUN_DIR/src/mojafos/commandline/commandline.sh" 

